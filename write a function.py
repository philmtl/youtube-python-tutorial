def say_hi(name, age): #the text inside the brackets are parameters, so when you call the function its will fill it with thoes
    print("hello " + name + " you are " + str(age))


say_hi("mike", 35)
say_hi("nick", 44)

